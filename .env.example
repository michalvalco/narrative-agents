# Copy to .env and fill in. Never commit .env.
# Anthropic API key — used by narrative_agents/llm_core.py (LLM-enhanced agents).
ANTHROPIC_API_KEY=
# ARC-AGI-3 API key — register at https://three.arcprize.org (Michal registers; sessions never handle the key).
ARC_API_KEY=
