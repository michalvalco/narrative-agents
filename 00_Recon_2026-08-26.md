# Recon — narrative-agents, ablation setup session

**Date:** 2026-08-26 · **Branch:** `ablation-setup` (created this session off `main` @ 603fd26) · **Executor:** Claude Code (Fable 5), Desktop machine.
*Filename note:* the setup prompt names this file `00_Recon_2026-08-22.md`; dated 2026-08-26 because that is when the recon actually ran (a backdated record would be a false one).

## Repo state at session start (the ten lines)

1. Branch `main`, clean tree, **1 commit ahead of origin** (603fd26, the hooks reference — unpushed, push is Michal's call).
2. Also exists: branch `main-MichalDesktop` (local + origin), untouched this session.
3. `.env` present (333 B), covered by `.gitignore` line 99, **never tracked in git history** (verified `git log --all -- .env` → empty).
4. `.env.example` did **not** exist on disk and was gitignored (499d03f) — contradicting Task C; resolved this session in favor of Task C (placeholder committed, ignore line removed).
5. `_Archive/` (96 files incl. neutralized `.git`, `.docx` drafts) was **tracked and pushed** in ed96e9c, against `_Archive/README.md`'s instruction — untracked this session via `git rm -r --cached` (Gotchas #290); blobs remain in history, no rewrite.
6. `STATE.md` absent → created this session (Harness_Design §3.1 five-part schema; this repo is its first live specimen).
7. Live code: `narrative_agents/` (core.py 381, llm_core.py 232, prompts.py 81 lines), 5 example scripts, docs/, no `tests/` directory — **zero test coverage in the live repo** (the only test file ever written sits in `_Archive/.../old files/tests/test_narrative_agents.py`, written against the October 2025 core).
8. `Sources/` held only `claude_code_hooks_reference.md` (Task B item 6, done 2026-08-25).
9. `.claude/` exists but is empty; no CLAUDE.md in the repo.
10. No venv existed; `requirements.txt` has no pytest (commented out).

## Where the 0.7 threshold lives

- **`narrative_agents/core.py:100`** — `if relevance > 0.7:` → core; else peripheral. The only storage gate.
- **`narrative_agents/prompts.py:36`** — `RELEVANCE_SYSTEM_PROMPT` **announces the threshold to the scorer**: "0.7+ = identity-forming (will become a core memory)."
- **`narrative_agents/llm_core.py:147–169`** — `LLMNarrativeAgent._assess_relevance` sends one experience in isolation to the fast model (`max_tokens=10`), parses a single decimal, clamps to [0,1]; falls back to the rule-based scorer on parse failure or API error.
- Feedback loop: `_update_character` (core.py:181) fires **only on core entry**; `_character_summary` (llm_core.py:99) feeds character back into every relevance call → the cold-start bistability Experiment_Spec §6 diagnoses.

## The 0/10 artifact (run log, "Narrative agents experiment 01 …")

LLM identity formation, 10 experiences (6 error/neutral-heavy): Scholar **4/10** core, Performer **0/10**, Explorer **0/10** (log lines 200–210). Per Experiment_Spec §6 this is an artifact of the absolute threshold + biased experience set, not a finding.

## Model configuration

The v2 scripts load `.env` (dotenv) for `ANTHROPIC_API_KEY` only. Model names are **hard-coded defaults** in `llm_core.py:27–29`: fast `claude-haiku-4-5-20251001`, reasoning `claude-sonnet-4-6`; overridable per-constructor, not per-.env.

## Environment (built this session)

`.venv` via `uv` on Python 3.13.5; installed `arc-agi==0.9.9` (the exact pin in `vendor/arc-skill/README.md:124` and `arc_cli.py:5`), numpy, pillow, pytest, python-dotenv, anthropic, colorama, matplotlib, rich. Offline smoke test passed: `Arcade(arc_api_key=None, operation_mode=OperationMode.OFFLINE, recordings_dir='runs')` instantiates with `socket.connect` monkey-blocked — no key, no network, no scorecard opened. `OperationMode` values confirmed: NORMAL / ONLINE / OFFLINE / COMPETITION. `Arcade.__init__` params: `arc_api_key, arc_base_url, operation_mode, environments_dir, recordings_dir, logger`.

## vendor/arc-skill (read-only reference)

Cloned at commit `dba53c3799eab600a512dd73ed037d7ab6958c66`; gitignored. `skills/arc-skill/SKILL.md` = 129 lines (matches the documented figure). Detailed notes: `Sources/arc-skill_notes.md`.
